NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Appetize.io API token — SERVER-SIDE ONLY (no NEXT_PUBLIC_ prefix, never sent
# to the browser). Used to manage app builds via the Appetize REST API. The
# in-browser embed uses a per-app publicKey instead, which is safe to expose.
APPETIZE_API_TOKEN=
